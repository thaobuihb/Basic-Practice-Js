// Create a loop to log all elements of the array to the console.
let toys = ["kite", "car", "bike"];
for (let i = 0; i < toys.length; i++) {
  console.log(toys[i]);
}

// Create an array toys that has value: ['kite', 'car', 'bike']
// Create initialization: let i = 0
// Create condition: i < toys.length
// Create final-expression: i++
// Create a statement to log all the elements.
// Expected result logged to the console: kite car bike
