// Declare function createLearner() and a variable school.
const school = "CoderSchhol";
function createLearner(name, age) {
  return `${name} is ${age} years old at ${school}`;
}
console.log(createLearner("Thao", 36));

// 1)school is a constant and assigned a value: CoderSchhol
// 2)createLearner() takes 2 inputs: name, age
// 3) createLearner() returns a string: (Name) is (age) years old at (school)
