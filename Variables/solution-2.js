// Declare 3 variables called myNumber, myName, mySchool

// Requirements:

// myNumber stores a boolean value: true
let myNumber = true;
// myName can’t be declared again and stores a string value: abc123
const myName = "abc123";
// mySchool can be declared again and stores no values.
let mySchool;
